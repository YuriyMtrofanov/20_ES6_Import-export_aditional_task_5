export class DataContainer {

    // класс на основе которого создаётся объект с данными полученными из текстового поля
    constructor(date, amount){
        this.date = date,
        this.amount = amount
    }
}